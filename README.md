# Data_cleaning.sqlSELECT * FROM DATA;
SELECT COUNT(*)AS TOTAL_CUSTOMERS
FROM data;
SELECT SUM("AMOUNT") AS TOTAL_SALES
FROM data;

SELECT "COUNTRY",SUM("AMOUNT") AS TOTAL_SALES FROM DATA GROUP BY "COUNTRY";
SELECT AVG("AGE") AS AVERAGE_AGE FROM DATA;
SELECT "PRODUCT",SUM("AMOUNT")AS TOTAL_SALES FROM DATA GROUP BY"PRODUCT";
SELECT"YEAR",SUM("AMOUNT")AS TOTAL_SALES FROM DATA GROUP BY"YEAR";
SELECT MAX("AMOUNT")AS HIGHEST_AMOUNT FROM DATA;
SELECT MIN("AMOUNT") AS LOWEST_AMOUNT FROM DATA ;
SELECT*FROM DATA ORDER BY"AMOUNT" DESC LIMIT 5;

SELECT"CUSTOMER NAME",SUM("AMOUNT") AS TOTAL_SPENT 
FROM DATA 
GROUP BY"CUSTOMER NAME"
ORDER BY TOTAL_SPENT DESC
LIMIT 5;

SELECT"COUNTRY",AVG("AMOUNT")AS
AVERAGE_SALES FROM DATA 
GROUP BY "COUNTRY"
ORDER BY AVERAGE_SALES DESC;

SELECT"YEAR",SUM("AMOUNT") AS
TOTAL_SALES
FROM DATA 
GROUP BY "YEAR"
ORDER BY "YEAR";

SELECT"YEAR","PRODUCT",
SUM("AMOUNT") AS TOTAL_SALES
FROM DATA
GROUP BY"YEAR","PRODUCT"
ORDER BY"YEAR",TOTAL_SALES DESC;

SELECT "CUSTOMER NAME","AMOUNT"
FROM DATA 
ORDER BY "AMOUNT"DESC
LIMIT 1; 


SELECT* FROM DATA LIMIT 5;

SELECT"CUSTOMER NAME","AGE","COUNTRY"
FROM DATA 
WHERE "AGE">30;

SELECT "COUNTRY",SUM("AMOUNT")AS 
TOTAL_SALES
FROM DATA 
GROUP BY "COUNTRY"
HAVING SUM ("AMOUNT")>5000;

SELECT "COUNTRY",COUNT(*) AS 
CUSTOMER_COUNT
FROM DATA 
GROUP BY "COUNTRY"
ORDER BY CUSTOMER_COUNT DESC;

SELECT COUNT(*)FROM DATA;

SELECT SUM("AMOUNT")FROM DATA;


SELECT*FROM DATA
WHERE "CUSTOMERCODE" IS NULL
OR "CUSTOMERNAME" IS NULL 
OR COUNTRY IS NULL 
OR AGE IS NULL 
OR PRODUCT IS NULL 
OR AMOUNT IS NULL 
OR YEAR IS NULL;

SELECT CUSTOMERCODE , COUNT(*)
FROM DATA 
GROUP BY CUSTOMERCODE 
HAVING COUNT(*)>1;

SELECT*FROM DATA 
WHERE CUSTOMERNAME !=
TRIM(CUSTOMERNAME);

SELECT* FROM DATA 
WHERE COUNTRY != TRIM(COUNTRY);

SELECT * FROM DATA  
WHERE AGE < 0 OR AGE > 100;

SELECT* FROM DATA 
WHERE AMOUNT <=0;

SELECT * FROM DATA 
WHERE YEAR < 2000 OR YEAR > 2030;



